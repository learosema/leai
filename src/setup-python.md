# Setting up Python

Install and set the python version to `3.10` 

```sh
pyenv install 3.10
pyenv global 3.10
```

## Initialize a virtual environment

```sh
python3 -m venv venv
source .venv/bin/activate
python3 -m pip install --upgrade pip setuptools
```

