# Classifier

Classifier is a model that learns to categorize vectors into predefined categories based on their features. The categorizations may be probabilistic in nature, assigning more probability to labels that have higher confidence.

